# Promise All

## 📊 Question Info

- **Submitted**: 7/6/2026, 8:28:51 PM
- **Difficulty**: Medium
- **Tags**: javascript
- **Link**: [https://www.greatfrontend.com/interviews/study/gfe75/questions/javascript/promise-all](https://www.greatfrontend.com/interviews/study/gfe75/questions/javascript/promise-all)

## 💻 Implementation

```javascript
export default function promiseAll<T extends readonly unknown[] | []>(
  iterable: T,
): Promise<{ -readonly [P in keyof T]: Awaited<T[P]> }> {
  return new Promise((resolve, reject) => {
    const result: T[] = [];
    let count = 0;

    if (iterable.length === 0) {
      resolve([] as any);
      return;
    }

    iterable.forEach((item: any, index) => {
      Promise.resolve(item)
        .then((value) => {
          result[index] = value;
          count++;

          if (count === iterable.length) {
            resolve(result as any);
          }
        })
        .catch((err) => reject(err));
    });
  });
}

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
