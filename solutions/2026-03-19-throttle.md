# Throttle

## 📊 Question Info

- **Submitted**: 3/18/2026, 6:02:54 PM
- **Link**: [https://www.greatfrontend.com/questions/javascript/throttle?practice=practice&tab=coding](https://www.greatfrontend.com/questions/javascript/throttle?practice=practice&tab=coding)

## 💻 Implementation

```javascript
/**
 * @callback func
 * @param {number} wait
 * @return {Function}
 */

export default function throttle(func, wait) {
  //use a flag to track if the function is in cooldown
  let isThrottle = false;

  //return a new function that wraps the original logic
  return function (...args) {
    //if we are currently in cooldown, ignore the call
    if (isThrottle) return;

    //excute the original function immediately
    //use apply to maintain the correct this context
    func.apply(this, args);

    //enter the cooldown mode
    isThrottle = true;

    //after the wait duration, reset the flag
    setTimeout(() => {
      isThrottle = false;
    }, wait);
  };
}

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
