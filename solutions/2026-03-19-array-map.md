# Array Map

## 📊 Question Info

- **Submitted**: 3/18/2026, 9:53:15 PM
- **Link**: [https://www.greatfrontend.com/questions/javascript/array-map?practice=practice&tab=coding](https://www.greatfrontend.com/questions/javascript/array-map?practice=practice&tab=coding)

## 💻 Implementation

```javascript
 * @param { (value: T, index: number, array: Array<T>) => U } callbackFn
 * @param {any} [thisArg]
 * @return {Array<U>}
 */
Array.prototype.myMap = function (callbackFn, thisArg) {
  //get the length of original array(this)
  const length = this.length;

  //create a new array to store the result;
  const result = new Array(length);

  //iterate throgh the array
  for (let i = 0; i < length; i++) {
    //handle sparse array: check if the index exist in the array
    if (Object.prototype.hasOwnProperty.call(this, i)) {
      //call the callback function with(element, index, originalArray)
      //use .call to apply thisArg if provided
      result[i] = callbackFn.call(thisArg, this[i], i, this);
    }
  }

  return result;
};

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
