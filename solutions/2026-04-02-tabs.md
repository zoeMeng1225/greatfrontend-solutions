# Tabs

## 📊 Question Info

- **Submitted**: 4/2/2026, 10:59:59 AM
- **Difficulty**: Medium
- **Tags**: user-interface
- **Link**: [https://www.greatfrontend.com/interviews/study/gfe75/questions/user-interface/tabs](https://www.greatfrontend.com/interviews/study/gfe75/questions/user-interface/tabs)

## 💻 Implementation

```javascript
import { useState } from "react";

export default function Tabs() {
  //initialize the state to track the active tab index
  const [active, setActive] = useState(0);
  const tabData = [
    {
      label: "HTML",
      content:
        "The HyperText Markup Language or HTML is the standard markup language for documents designed to be displayed in a web browser.",
    },
    {
      label: "CSS",
      content:
        "Cascading Style Sheets is a style sheet language used for describing the presentation of a document written in a markup language such as HTML or XML.",
    },
    {
      label: "JavaScript",
      content:
        "JavaScript, often abbreviated as JS, is a programming language that is one of the core technologies of the World Wide Web, alongside HTML and CSS.",
    },
  ];

  return (
    <div>
      <div>
        {tabData.map((tab, index) => (
          <button
            key={index}
            onClick={() => setActive(index)}
            style={{ color: active === index ? "blue" : "black" }}
          >
            {tab.label}
          </button>
        ))}
      </div>
      <div style={{ marginTop: "20px" }}>
        <p>{tabData[active].content}</p>
      </div>
    </div>
  );
}

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
