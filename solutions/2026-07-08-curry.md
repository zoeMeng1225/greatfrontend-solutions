# Curry

## 📊 Question Info

- **Submitted**: 7/7/2026, 7:39:07 PM
- **Difficulty**: Medium
- **Tags**: javascript
- **Link**: [https://www.greatfrontend.com/questions/javascript/curry](https://www.greatfrontend.com/questions/javascript/curry)

## 💻 Implementation

```javascript
/**
 * @param {(...args: Array<unknown>) => unknown} func
 * @returns {(...args: Array<unknown>) => unknown}
 */
export default function curry(func) {
  //define a helper function to collect arguments
  //...args(rest parameter) it collects all remaining arguments passed to a function into
  //a true Javascript Array. Becuase it 's a real array, you can directly use all array method on it,
  //like .map(), filter(), or .length

  return function curried(...args) {
    //check if the number of args provided is enough
    if (args.length >= func.length) {
      //base case: enough args, execute the orignal function
      return func.apply(this, args);
    }

    //if not enought: return new function to collect more
    //bind create a new function, this point the original one
    return curried.bind(this, ...args);
  };
}

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
