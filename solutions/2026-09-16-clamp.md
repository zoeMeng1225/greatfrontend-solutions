# Clamp

## 📊 Question Info

- **Submitted**: 9/15/2026, 8:56:15 PM
- **Difficulty**: Easy
- **Tags**: javascript
- **Link**: [https://www.greatfrontend.com/questions/javascript/clamp?practice=practice&tab=coding](https://www.greatfrontend.com/questions/javascript/clamp?practice=practice&tab=coding)

## 💻 Implementation

```javascript
/**
 * @param {number} value The number to clamp.
 * @param {number} lower The lower bound.
 * @param {number} upper The upper bound.
 * @returns {number} Returns the clamped number.
 */
export default function clamp(value, lower, upper) {
  // let maxNum = Math.max(value, lower, upper);
  // let minNum = Math.min(value, lower, upper)
  // console.log(value, lower, upper)
  let newArray = new Array(value, lower, upper);
  // let makeFilter = newArray.filter(num => num !== maxNum && num != minNum)

  // return makeFilter[0]
  newArray.sort((a, b) => a - b);
  return newArray[1]

  // console.log(makeArray)
}

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
