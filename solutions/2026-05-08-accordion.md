# Accordion

## 📊 Question Info

- **Submitted**: 5/7/2026, 10:02:18 PM
- **Difficulty**: Easy
- **Tags**: user-interface
- **Link**: [https://www.greatfrontend.com/interviews/study/gfe75/questions/user-interface/accordion](https://www.greatfrontend.com/interviews/study/gfe75/questions/user-interface/accordion)

## 💻 Implementation

```javascript
import { useState } from "react";

export default function Accordion() {
  const [htmlBox, setHtmlBox] = useState(false);
  const [cssBox, setcssBox] = useState(false);
  const [jsBox, setJsBox] = useState(false);

  const hanlder = (state, setState) => {
    setState(!state)
  }

  return (
    <div>
      <div>
        <button className="html button" onClick={() => hanlder(htmlBox, setHtmlBox)}>
          HTML <span aria-hidden={true} className="accordion-icon" />
        </button>
        <div style={htmlBox ? {display: 'block'}: {display:'none'}}>
          The HyperText Markup Language or HTML is the standard markup language
          for documents designed to be displayed in a web browser.
        </div>
      </div>
      <div>
        <button className="button" onClick={() => hanlder(cssBox, setcssBox)}>
          CSS <span aria-hidden={true} className="accordion-icon" />
        </button>
         <div style={cssBox ? {display: 'block'}: {display:'none'}}>
          Cascading Style Sheets is a style sheet language used for describing
          the presentation of a document written in a markup language such as
          HTML or XML.
        </div>
      </div>
      <div>
        <button className="button" onClick={() => hanlder(jsBox, setJsBox)}>
          JavaScript <span aria-hidden={true} className="accordion-icon" />
        </button>
         <div style={jsBox ? {display: 'block'}: {display:'none'}}>
          JavaScript, often abbreviated as JS, is a programming language that is
          one of the core technologies of the World Wide Web, alongside HTML and
          CSS.
        </div>
      </div>
    </div>
  );
}

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
