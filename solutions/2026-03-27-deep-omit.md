# Deep Omit

## 📊 Question Info

- **Submitted**: 3/27/2026, 1:24:42 PM
- **Difficulty**: Medium
- **Tags**: javascript
- **Link**: [https://www.greatfrontend.com/questions/javascript/deep-omit?practice=practice&tab=coding](https://www.greatfrontend.com/questions/javascript/deep-omit?practice=practice&tab=coding)

## 💻 Implementation

```javascript
/**
 * @param {any} val
 * @param {Array<string>} keys
 * @returns any
 */
export default function deepOmit(val, keys) {
  //handle null or primitives: return as is
  if (val === null || typeof val !== "object") {
    return val;
  }

  //handle Arrays: recursively process each element
  if (Array.isArray(val)) {
    return val.map((item) => deepOmit(item, keys));
  }

  //handle Objects: filter keys and recurse on values
  const result = {};
  //use a Set for O(1) lookup performance
  const keySet = new Set(keys);
  Object.keys(val).forEach((key) => {
    //if key is in the blacklist, skip it
    if (keySet.has(key)) {
      return;
    }

    //otherwise, resursively process the value
    result[key] = deepOmit(val[key], keys);
  });

  return result;
}

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
