# Promise Any

## 📊 Question Info

- **Submitted**: 3/24/2026, 2:19:43 PM
- **Difficulty**: Medium
- **Tags**: javascript
- **Link**: [https://www.greatfrontend.com/questions/javascript/promise-any?practice=practice&tab=coding](https://www.greatfrontend.com/questions/javascript/promise-any?practice=practice&tab=coding)

## 💻 Implementation

```javascript
/**
 * @param {Array} iterable
 * @return {Promise}
 */
export default function promiseAny(iterable) {
  return new Promise((resolve, reject) => {
    //if the array is empty, reject immediately
    if (iterable.length === 0) {
      return reject(new AggregateError([], "All promise were rejected"));
    }

    let rejectedCount = 0;
    const errors = new Array(iterable.length);

    iterable.forEach((item, index) => {
      //wrap item in Promise.resolve to handle non-promise values
      Promise.resolve(item).then(
        (value) => {
          //success: if any promise fulfills, resolve the whole thing
          resolve(value);
        },
        (error) => {
          //failure: keep track of errors and theirs and original positions
          errors[index] = error;
          rejectedCount++;

          //if all promises fail, reject with AggregateError
          if (rejectedCount === iterable.length) {
            reject(new AggregateError(errors, "All promises were rejected"));
          }
        },
      );
    });
  });
}

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
