# Promise All Settled

## 📊 Question Info

- **Submitted**: 7/24/2026, 9:15:23 PM
- **Difficulty**: Medium
- **Tags**: javascript
- **Link**: [https://www.greatfrontend.com/questions/javascript/promise-all-settled?practice=practice&tab=coding](https://www.greatfrontend.com/questions/javascript/promise-all-settled?practice=practice&tab=coding)

## 💻 Implementation

```javascript
/**
 * @param {Array} iterable
 * @return {Promise<Array<{status: 'fulfilled', value: *}|{status: 'rejected', reason: *}>>}
 */
export default function promiseAllSettled(iterable) {
  return new Promise((resolve) => {
    const result = [];
    let completeCount = 0;

    if (iterable.length === 0) {
      resolve([]);
      return;
    }

    iterable.forEach((item, index) => {
      Promise.resolve(item)
        .then((value) => {
          result[index] = { status: "fulfilled", value };
        })
        .catch((reason) => {
          result[index] = { status: "rejected", reason };
        })
        .finally(() => {
          completeCount++;

          if (completeCount === iterable.length) {
            resolve(result);
          }
        });
    });
  });
}

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
