# Use State With Reset

## 📊 Question Info

- **Submitted**: 6/17/2026, 9:56:17 PM
- **Difficulty**: Easy
- **Tags**: javascript
- **Link**: [https://www.greatfrontend.com/questions/javascript/use-state-with-reset?practice=practice&tab=coding](https://www.greatfrontend.com/questions/javascript/use-state-with-reset?practice=practice&tab=coding)

## 💻 Implementation

```javascript
import { useState, useRef, useCallback } from "react";

export default function useStateWithReset(initialStateOrInitializer) {
  //use useState to manage the current value
  const [state, setState] = useState(initialStateOrInitializer);

  //use a ref to lock the very first value as our reset target;
  const initialStateRef = useRef(state);

  //the reset function sets the state back to the stored initial value
  const reset = useCallback(() => {
    setState(initialStateRef.current);
  }, []);

  //return the valua, setter, and reset function as an array;
  return [state, setState, reset];
}

```

---

*Auto-generated by [GreatFrontEnd GitHub Sync](https://github.com)*
